<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LeetCode Flex</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Global styles */
    body {
      background-color: #121212; /* Dark background */
      color: #00a2ff; /* LeetCode blue color */
      font-family: Arial, sans-serif;
      height: 90vh; /* Full viewport height */
      display:flex;
      justify-content: center;
      align-items: center; /* Centering the container */
      margin: 0;
    }

    h1 {
      color: #f2994a; /* LeetCode orange color */
      font-size: 3rem;
    }

    .alert {
      color: #fff;
    }

    .btn-primary {
      background-color: #f2994a; /* Dark orange */
      border: none;
      box-shadow: 0 0 10px rgba(242, 153, 74, 0.5);
      font-size: 1.2rem;
      margin-top: 45px;
    }

    .btn-primary:hover {
      background-color: #f28c39; /* Darker orange */
      box-shadow: 0 0 25px rgba(242, 153, 74, 0.7);

    }

    .container {
      background-color: #1c1c1c; /* Dark background for the card */
      border-radius: 10px;
      padding: 40px;
      max-width: 600px;
      width: 100%;
      text-align: center;
    }

    .logo {
      width: 180px;
      margin-bottom: 30px;
    }

    .form-control {
      background-color: #2c2c2c;
      color: #fff;
      border: 1px solid #444;
      font-size: 1.2rem;
      padding: 10px;
    }

    .form-control:focus {
      border-color: #00a2ff;
      box-shadow: 0 0 5px rgba(0, 162, 255, 0.5);
    }

    /* Placeholder style */
    .form-control::placeholder {
      color: #f2994a; /* LeetCode orange color */
      font-size: 1.2rem;
    }

    /* Hide alt text if image fails to load */
    img[alt] {
      visibility: hidden;
    }
  </style>
</head>
<body>

  <div class="container mt-5">
    <!-- LeetCode Logo -->
    <img src="leetcode.jpg" alt="LeetCode" class="logo">

    <h1 class="mb-4">LeetCode Profile Card</h1>

    <% if (success && success.length > 0) { %>
      <div class="alert alert-success text-center"><%= success[0] %></div>
    <% } %>

    <% if (error && error.length > 0) { %>
      <div class="alert alert-danger text-center"><%= error[0] %></div>
    <% } %>

    <form action="/profile" method="POST" class="d-flex flex-column align-items-center">
      <input type="text" name="username" class="form-control w-75 mb-3" placeholder="Enter LeetCode username or profile URL" required />
      <button type="submit" class="btn btn-primary">Generate Card</button>
    </form>
  </div>

</body>
</html>
